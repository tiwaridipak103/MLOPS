How to run tox file:
Remove any cached tox artifacts and re-run:

tox -r
The -r flag forces tox to recreate the environments.

--------------------------------------------------------------

Steps to Upgrade the Database Schema
Take a Backup
Since schema migrations might modify the database structure, it’s recommended to back up your database before proceeding. You can copy the mlflow.db file to a safe location:

cp mlflow.db mlflow_backup.db
Run the Upgrade Command
Use the mlflow db upgrade command to update the schema:

mlflow db upgrade sqlite:///mlflow.db
Start the MLflow Server Again
After the schema upgrade, try starting the MLflow server:

bash
Copy code
mlflow server --backend-store-uri sqlite:///mlflow.db --default-artifact-root ./mlruns --host 127.0.0.1 --port 5000
Verify the Fix
Open a browser and navigate to http://127.0.0.1:5000 to ensure the MLflow UI is running correctly.

----------------------------------------------------------------------------------------------------------------------------

mlflow server --backend-store-uri sqlite:///mlflow.db \
              --default-artifact-root file:DEMO_APPLICATION/mlruns \
              --host 127.0.0.1 --port 5000
              

---------------------------------------------------------------------------------------------------------
The error indicates that port 5000 is already in use, likely because another process is running on it. Here's how you can address this issue:

Steps to Resolve
Identify the Process Using Port 5000: Run the following command to find the process:

bash
Copy code
lsof -i :5000
This will show the process ID (PID) of the application using the port.

Terminate the Process: After identifying the PID, terminate it using:

bash
Copy code
kill -9 <PID>
Replace <PID> with the actual process ID.


-----------------------------------------------------------------------------------------------
mkdir -p /mnt/d/Credit_Risk_with_Mlops/Mlproject/Zenml/DEMO_APPLICATION/mlruns
chmod -R 777 /mnt/d/Credit_Risk_with_Mlops/Mlproject/Zenml/DEMO_APPLICATION/mlruns

----------------------------------------------------------------------------------

mlflow server --backend-store-uri sqlite:////mnt/d/Credit_Risk_with_Mlops/Mlproject/Zenml/DEMO_APPLICATION/mlruns/mlflow.db \
                --default-artifact-root file:///mnt/d/Credit_Risk_with_Mlops/Mlproject/Zenml/DEMO_APPLICATION/mlruns \
                --host 127.0.0.1 --port 5000

/media/tiwaridipak103/DC68B3D168B3A8A2/Credit Risk with Mlops/Mlproject/Zenml/DEMO_APPLICATION
